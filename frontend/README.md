# 🎬 Wink PreViz - Генератор Storyboard

![Wink PreViz](./public/images/wink-logo.webp)

Веб-приложение для автоматической генерации storyboard из сценариев.

---

## 🚀 Быстрый старт

### 1. Установка

```bash
npm install
```

### 2. Запуск

```bash
npm run dev
```

Откройте http://localhost:5173 в браузере.

---

## 📱 Функционал

### ✅ Реализовано:

1. **📄 Загрузка сценария** (PDF/DOCX)
2. **🎬 Редактирование сцен** (локации, персонажи, реквизит)
3. **🎨 Генерация storyboard** с тремя уровнями детализации:
   - Эскиз (ч/б)
   - Средняя детализация (цвет)
   - Финальный кадр (детализированный)
4. **✏️ Редактирование промптов** для генерации
5. **📜 История генераций** для каждой сцены
6. **📥 Экспорт** в PDF

### 🎨 Дизайн:
- Фирменный стиль Wink (цвета, градиенты, шрифты)
- Темная тема
- Адаптивный интерфейс

---

## 🔧 Технологии

- **React 19** - UI библиотека
- **Vite 7** - сборщик
- **TailwindCSS 3** - стили
- **React Router** - навигация
- **Axios** - HTTP клиент
- **Lucide React** - иконки

---

## 📂 Структура

```
wink-previz/
├── src/
│   ├── components/          # Компоненты экранов
│   │   ├── UploadScene.jsx     # Загрузка файла
│   │   ├── SceneList.jsx       # Редактирование сцен
│   │   └── FrameViewer.jsx     # Генерация кадров
│   ├── api/
│   │   └── apiClient.js        # API клиент
│   ├── utils/
│   │   └── mockData.js         # Тестовые данные
│   └── pages/
│       └── MainPage.jsx        # Главная страница
├── public/
│   ├── fonts/               # Шрифты Wink
│   └── images/              # Логотипы
└── README.md
```

---

## 🔌 API Интеграция

### Настройка Backend URL

В файле `src/api/apiClient.js` измените:

```javascript
const API_BASE_URL = 'http://localhost:8080/api'; // ← Ваш URL
```

### Необходимые эндпоинты:

```
POST   /api/scripts/upload              - Загрузка сценария
GET    /api/scripts/{id}/scenes         - Получить сцены
PUT    /api/scenes/{id}                 - Обновить сцену
POST   /api/scenes/{id}/generate        - Сгенерировать кадр
POST   /api/frames/{id}/regenerate      - Регенерировать кадр
GET    /api/scenes/{id}/frames          - История генераций
GET    /api/scripts/{id}/export         - Экспорт PDF
DELETE /api/scenes/{id}                 - Удалить сцену
POST   /api/scripts/{id}/scenes         - Добавить сцену
```

### CORS

Не забудьте настроить CORS на бэкенде:

```javascript
// Пример для Express.js
app.use(cors({
  origin: 'http://localhost:5173'
}));
```

---

## 🧪 Тестирование без бэкенда

Приложение работает с mock данными из коробки!

В правом верхнем углу есть **DEV меню** для быстрой навигации:
- Экран 1: Загрузка
- Экран 2: Редактирование
- Экран 3: Генерация

Просто нажимайте кнопки для переключения между экранами.

---

## 📦 Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `dist/`.

### Деплой:

**Vercel / Netlify:**
```bash
npm install -g vercel
vercel
```

**Docker:**
```bash
docker build -t wink-previz .
docker run -p 80:80 wink-previz
```

---

## 🎯 Использование

### 1. Запустите приложение
```bash
npm run dev
```

### 2. Откройте в браузере
http://localhost:5173

### 3. Тестируйте
- Используйте **DEV меню** справа вверху для навигации
- Загрузите любой файл или нажмите кнопки для просмотра демо
- Все работает с mock данными

---

## 📞 Техническая информация

### Конфигурация

- `vite.config.js` - настройки Vite и прокси для API
- `tailwind.config.js` - цвета и стили Wink
- `postcss.config.js` - PostCSS конфигурация
- `package.json` - зависимости

### Основные команды

```bash
npm install      # Установка зависимостей
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
npm run preview  # Предпросмотр сборки
```

---

## 🎨 Дизайн-система Wink

### Цвета
```css
--wink-black: #000000       /* Фон */
--wink-dark: #171717        /* Карточки */
--wink-gray: #464645        /* Границы */
--wink-orange: #ff5b21      /* Акцент */
```

### Градиент
```css
background: linear-gradient(27.5deg, #FF9532 0%, #FF8A1C 31%, #FF5B21 46%, #FF5A24 67%);
```

### Шрифты
- **Заголовки:** CoFoKak-Black
- **Текст:** CoFoSans

---

## ⚠️ Важно

- Для полной функциональности нужен настроенный бэкенд
- Без бэкенда работает в демо-режиме с mock данными
- Убедитесь что CORS настроен на бэкенде

---

## 📄 Лицензия

Разработано для хакатона **Wink 2025** в партнерстве с **Codenrock**.

---

**Готово к использованию! 🚀**
