# 🧠 Интеллектуальный сервис превизуализации сценариев

## 🎯 Цель проекта
Создать **локальную (self-hosted) платформу превизуализации сценариев**, которая:
 - Принимает сценарий (PDF/DOCX);
 - Разбивает его на сцены и извлекает ключевой контекст (локации, персонажи, реквизит);
 - Позволяет выбрать сцену и сгенерировать визуализацию в одном из уровней детализации:
 - Эскиз (ч/б);
 - Средняя детализация (цвет);
 - Финальный кадр (детализированный, в художественном стиле);
 - Поддерживает правки и экспорт storyboard;
 - Работает **полностью офлайн**, без обращения к внешним API.

---

## 🧩 Архитектура системы

![Архитектурная схема решения]


📘 **Ключевая идея:** Spring Boot управляет логикой и хранением, Python отвечает за генерацию, а React — за UX.
Всё работает локально, без доступа к интернету.

---
![wink](https://github.com/user-attachments/assets/8a09a7d0-d773-46f9-80ae-4e0ee502baf2)

## ⚙️ Технологический стек

| Компонент         | Технологии                                                                  |
|-------------------|-----------------------------------------------------------------------------|
| **Frontend**      | React, TailwindCSS, Vite                                                    |
| **Backend**       | Java 17, Spring Boot, PostgreSQL, JPA/Hibernate                             |
| **AI-модули**     | Python 3.11, FastAPI, spaCy, Diffusers, Stable Diffusion / Flux, ControlNet |
| **Развёртывание** | Docker, Docker Compose                                                      |
| **Документация**  | Markdown, OpenAPI, README, Demo video                                       |

---

## 🪜 План реализации

### Этап 1. Базовая инфраструктура (день 1)
 - Инициализация репозитория и Docker Compose.
 - Каркас Spring Boot + React + FastAPI.
 - Подключение PostgreSQL и настройка схемы БД.
 - REST API: `/upload`, `/scenes`, `/generate`, `/export`.

### Этап 2. Импорт и парсинг сценария (день 2)
 - Конвертация PDF/DOCX → текст.
 - Сегментация на сцены (по INT./EXT. и метаданным).
 - NLP-анализ (персонажи, локации, реквизит, настроение).
 - Сохранение структуры сцен в БД.

### Этап 3. Генерация визуализаций (день 3–4)
 - Сбор промптов на основе контекста сцены.
 - Интеграция Stable Diffusion / Flux.
 - Поддержка 3 уровней детализации (эскиз, цвет, финал).
 - Генерация 2–4 вариантов на сцену.

### Этап 4. Правки и согласованность (день 4–5)
 - Continuity: сохранение seed, палитры, поз.
 - Img2Img и ControlNet для повторяемости.
 - Endpoint `/regenerate` для коротких правок.

### Этап 5. Веб-интерфейс (день 5–6)
 - Интерфейс со списком сцен и предпросмотром.
 - Кнопки LOD, перегенерации и экспорта.
 - Комментарии, история версий, PDF storyboard.

### Этап 6. Завершение и упаковка (день 7)
 - Финальное тестирование.
 - README + видео демо (до 5 мин).
 - Проверка офлайн-запуска и воспроизводимости.

---

## 📁 Структура репозитория

```
wink-previz/
│
├── backend/                         # Java Spring Boot
│   ├── src/main/java/com/wink/
│   │   ├── controller/              # REST API (upload, scenes, generate)
│   │   ├── service/                 # Бизнес-логика и оркестрация
│   │   ├── model/                   # Сущности (Script, Scene, Frame)
│   │   └── repository/              # JPA + PostgreSQL
│   ├── src/main/resources/
│   │   └── application.yml
│   └── Dockerfile
│
├── ai-modules/                      # Python FastAPI сервисы
│   ├── nlp_parser.py                # Сегментация и извлечение контекста
│   ├── prompt_builder.py            # Формирование промптов
│   ├── image_generator.py           # Stable Diffusion / Flux
│   ├── img2img_editor.py            # Исправления и continuity
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                        # Веб-интерфейс React
│   ├── src/
│   │   ├── components/
│   │   │   ├── UploadScene.jsx
│   │   │   ├── SceneList.jsx
│   │   │   ├── FrameViewer.jsx
│   │   │   └── EditPromptModal.jsx
│   │   ├── api/
│   │   │   └── apiClient.js
│   │   ├── pages/
│   │   │   └── MainPage.jsx
│   │   └── App.jsx
│   ├── package.json
│   └── Dockerfile
│
├── db/
│   └── init.sql                     # Скрипт инициализации БД
│
├── docker-compose.yml               # Все сервисы в одном окружении
├── README.md                        # Документация и инструкция запуска
└── demo/
    ├── sample_script.docx           # Пример сценария
    └── sample_storyboard.pdf        # Пример вывода
```

---

## 🧠 Ключевые функции MVP
 - Загрузка сценария (PDF/DOCX)
 - Сегментация и анализ сцен
 - Генерация 2–3 кадров (эскиз)
 - Текстовые правки и перегенерация
 - Экспорт storyboard в PDF
 - Полная офлайн-работа через Docker Compose

---

## ✅ Критерии оценки (по документу Wink AI Challenge)

| Критерий            | Вес     | Описание                                        |
|---------------------|---------|-------------------------------------------------|
| Полнота функционала | 30%     | Загрузка → сцены → генерация → правки → экспорт |
| Качество ИИ         | 25%     | Соответствие сценарию и стиль визуализаций      |
| UX                  | 20%     | Простота итераций и редактирования              |
| Автономность        | 15%     | Локальные модели, офлайн-запуск                 |
| Документация        | 10%     | Понятное описание и воспроизводимость           |

---

## 📦 Запуск проекта

```bash
git clone https://github.com/wbryon/wink-ai-previz
cd wink-ai-previz
docker compose up --build
```
После сборки платформа будет доступна локально по адресу:
**http://localhost:8080** (backend) и **http://localhost:3000** (frontend).

---

## 🎥 Демо
Пример пользовательского пути:
1. Загрузка сценария `sample_script.docx`.
2. Выбор сцены `EXT. SCHOOL YARD – MORNING`.
3. Генерация в режиме *эскиз* (3 варианта ракурса).
4. Правка текста: «рюкзак должен быть жёлтым».
5. Перегенерация и экспорт storyboard.

---

## 🔗 Интеграция со Script Processor (Python)

Backend больше не извлекает текст и не формирует чанки самостоятельно. Эти функции вынесены в отдельный модуль `script_processor` (FastAPI).

- Эндпоинт: `POST /split-script` (multipart `file`, query `model`)
- Результат: JSON c полями `chunks_dir`, `saved_files` и массивом `chunks`,
  где каждый элемент содержит: `chunk_index`, `token_count`, `text`.

Поток загрузки сценария в backend:
1) Файл сохраняется во внутреннее хранилище (`app.storage.upload-dir`).
2) Backend вызывает `script-processor /split-script` и получает тексты чанков.
3) Каждый чанк отправляется в AI-парсер (LLM) → получаем сцены.
4) Сцены сохраняются в БД, агрегируется сырой JSON-след.

### Переменные окружения (backend)
- `APP_STORAGE_UPLOAD_DIR` — директория для загрузок (по умолчанию `/data/uploads`).
- `AI_PARSER_BASE_URL` — базовый URL text-AI парсера (по умолчанию `http://ai:8000`).
- `AI_GENERATE_BASE_URL` — базовый URL генерации изображений (по умолчанию `http://ai:8000`).
- `PROMPT_COMPILER_BASE_URL` — сервис сборки промптов (по умолчанию `http://prompt-compiler:8010`).
- `SCRIPT_PROCESSOR_BASE_URL` — базовый URL `script_processor` (по умолчанию `http://script-processor:8000`).
- `SCRIPT_PROCESSOR_MODEL` — локальная модель токенайзера (по умолчанию `Qwen/Qwen3-32B`).
- `CORS_ALLOWED_ORIGINS` — список доменов для CORS (по умолчанию `http://localhost:5173`).
- `APP_RATE_LIMIT_PER_MINUTE` — лимит запросов генерации в минуту (по умолчанию `5`).

### Замечания
- Для успешной работы `script_processor` токенайзер выбранной модели должен быть доступен локально (см. `script_processor/models/...`).
- Swagger UI backend: `http://localhost:8080/api/v1/swagger-ui.html`.